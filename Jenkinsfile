pipeline {
    agent  any

    stages {
        stage('Build') {
            steps {
                echo 'Hello from GITHUB.com  - this is Build stage message'
                // bat 'my_batfile'
            }
        }
    }
}
